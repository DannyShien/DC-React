import React from 'react';

const DCP2 = (props) => {
    const numRolled = props.P2Roll; 
        return (
            <div>
                <div>Number rolled: {numRolled} </div>
            </div>
        )
}

export default DCP2;