import React from 'react';

const Button = (props) => {
    return (
        <div>
            <button 
                className = 'btn'
                >Roll Dice</button>
        </div>
    );
}

export default Button;